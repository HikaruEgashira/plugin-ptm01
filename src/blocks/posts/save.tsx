import * as React from "react";
import type { BlockSaveProps } from "@wordpress/blocks";
import type { Attribute } from ".";

const Save: React.FC<BlockSaveProps<Attribute>> = () => {
	return <a>hello</a>;
};

export default Save;
